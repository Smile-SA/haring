'use client';

import type { ReactElement, ReactNode } from 'react';

export interface I{{pascalCase name}}Props {
  children?: ReactNode;
}

export function {{pascalCase name}}(props: I{{pascalCase name}}Props): ReactElement {
  const { children } = props;
  return (
    <div>
      <h1>{{name}}</h1>
      {children}
    </div>
  );
}
