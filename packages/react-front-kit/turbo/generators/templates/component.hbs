import type { ReactNode } from 'react';

interface I{{pascalCase name}}Props {
  children?: ReactNode;
}

export function {{pascalCase name}}(props: I{{pascalCase name}}Props): JSX.Element {
  const { children } = props;
  return (
    <div>
      <h1>{{name}}</h1>
      {children}
    </div>
  );
}
